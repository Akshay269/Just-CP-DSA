#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;cin >> t;
    while (t--)
    {
        string s;cin >> s;
        string s1="aba";
        string s2="bab";
        size_t found = s.find(s1);
        if (found != string::npos)
        cout << "NO "<< endl;
        

       
    }
    return 0;
}