#include <bits/stdc++.h>
using namespace std;
int main()
{
    // int n, m, a, res;
    // cin >> n >> m >> a;
    // res = (n % a == 0 ? n / a : n / a + 1);
    // res *= (m % a == 0 ? m / a : m / a + 1);
    int t;cin>>t;
    while(t--){
        int x,n;cin>>x>>n;
        if(n%4==0) cout<<x<<"\n";
        else if(n%4==1) cout<<

    }

    return 0;  
}